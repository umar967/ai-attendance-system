# Install with:
#   python -m pip install -r requirements.txt
#
# Windows note:
# Do not add the PyPI package named "face-recognition" here. It forces pip to
# compile "dlib" from source on Windows. This project uses the prebuilt
# dlib-binary wheel plus the local face_recognition.py compatibility module.
#
# OpenCV note:
# ultralytics and mediapipe can pull different OpenCV packages. Keep both
# OpenCV wheels pinned to the same NumPy 1.26-compatible version to avoid the
# opencv-contrib-python >= 4.13 / NumPy >= 2 conflict.

# Core runtime
numpy==1.26.4
pandas==2.3.3
openpyxl==3.1.5
Pillow>=10,<13
requests>=2.32,<3
mysql-connector-python>=9,<10

# Computer vision / AI
opencv-python==4.10.0.84
opencv-contrib-python==4.10.0.84
ultralytics==8.4.2
mediapipe==0.10.14
dlib-binary==19.24.1
face-recognition-models==0.3.0
protobuf<5

# Evaluation / analysis
scipy==1.15.3
scikit-learn==1.7.2
matplotlib==3.10.8
seaborn==0.13.2
